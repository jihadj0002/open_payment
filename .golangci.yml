linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - gosec
    - gofmt
    - gocyclo
    - misspell

linters-settings:
  gocyclo:
    min-complexity: 15
  errcheck:
    check-type-assertions: true

run:
  timeout: 5m
  tests: true
