apiVersion: v1
kind: Secret
metadata:
  name: openpayment-secrets
  namespace: openpayment
type: Opaque
stringData:
  DATABASE_URL: "postgres://$(DB_USER):$(DB_PASSWORD)@$(DB_HOST):5432/paymentdb"
  REDIS_URL: "redis://$(REDIS_HOST):6379"
  JWT_SECRET: ""  # set via deploy pipeline
